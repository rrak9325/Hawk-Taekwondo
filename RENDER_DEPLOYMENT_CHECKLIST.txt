RENDER DEPLOYMENT CHECKLIST - FIXED ARCHITECTURE
================================================

CRITICAL CHANGES MADE:
----------------------
1. ✅ Changed from 2-service to 1-service architecture
   - Backend now serves the built frontend (correct approach)
   - Removed separate frontend service using vite preview
   
2. ✅ Fixed render.yaml build process
   - Builds frontend first (creates dist folder)
   - Then installs backend dependencies
   - Single startCommand runs backend which serves everything

3. ✅ Fixed app.js catch-all route
   - Now properly excludes /assets/ requests
   - Returns 404 for missing assets instead of index.html

4. ✅ Added Node version specification (.node-version)
   - Ensures consistent Node 18.x across builds

5. ✅ Added health check endpoint to render.yaml
   - Render will monitor /health endpoint


DEPLOYMENT STEPS:
-----------------

STEP 1: Delete old services in Render Dashboard
   - Go to https://dashboard.render.com
   - Delete "hawk-taekwondo-backend" service (if it exists)
   - Keep or delete "hawk-taekwondo" service (we'll recreate it)

STEP 2: Push code to GitHub
   ```
   git add .
   git commit -m "Fix Render deployment architecture"
   git push origin main
   ```

STEP 3: Create new service in Render
   Option A - Using Blueprint (RECOMMENDED):
   - Click "New +" → "Blueprint"
   - Select your repository
   - Render will read render.yaml automatically
   
   Option B - Manual:
   - Click "New +" → "Web Service"
   - Connect repository
   - Name: hawk-taekwondo
   - Root Directory: (leave empty)
   - Build Command: (copy from render.yaml)
   - Start Command: cd backend && NODE_ENV=production node server.js

STEP 4: Set environment variables in Render Dashboard
   Required variables (set these manually):
   - ADMIN_USERNAME=your_admin_username
   - ADMIN_PASSWORD_HASH=your_bcrypt_hash
   - CLOUDINARY_CLOUD_NAME=your_cloud_name
   - CLOUDINARY_API_KEY=your_api_key
   - CLOUDINARY_API_SECRET=your_api_secret
   
   Auto-set by render.yaml:
   - NODE_ENV=production
   - PORT=10000
   - FRONTEND_URL=https://hawktaekwondo.onrender.com

STEP 5: Deploy
   - Click "Manual Deploy" → "Deploy latest commit"
   - Watch the logs for:
     ✅ "=== Building Frontend ==="
     ✅ "=== Frontend build complete ==="
     ✅ "=== Installing Backend Dependencies ==="
     ✅ "SERVER STARTED"
     ✅ "✅ Serving static files from: /opt/render/project/src/frontend/dist"

STEP 6: Verify deployment
   - Visit https://hawktaekwondo.onrender.com
   - Check browser console for errors
   - Test navigation (should load without MIME errors)
   - Check /health endpoint: https://hawktaekwondo.onrender.com/health


TROUBLESHOOTING:
----------------

ERROR: "Frontend dist folder not found"
FIX: Check build logs - frontend build may have failed
     Verify package.json and package-lock.json are committed

ERROR: "Failed to scan for dependencies"
FIX: This was caused by old 2-service architecture
     New architecture fixes this completely

ERROR: MIME type errors in browser
FIX: Already fixed in app.js catch-all route
     Clear browser cache and hard refresh (Ctrl+Shift+R)

ERROR: "SIGTERM" during startup
FIX: This was caused by vite preview in production
     New architecture uses proper Node.js server


ARCHITECTURE EXPLANATION:
-------------------------

OLD (BROKEN):
  Service 1: Frontend (vite preview) ❌
  Service 2: Backend (Express)       ❌
  Problem: vite preview is not for production

NEW (CORRECT):
  Service 1: Backend (Express)       ✅
    - Serves API at /api/*
    - Serves uploads at /uploads/*
    - Serves built frontend (SPA) for all other routes
  
  Build process:
    1. Build frontend → creates frontend/dist/
    2. Install backend deps
    3. Start backend → serves everything


COST:
-----
- 1 service = FREE (750 hours/month)
- Cloudinary = FREE (25GB storage)
- Total: $0/month


NEXT STEPS AFTER DEPLOYMENT:
-----------------------------
1. Test all pages and features
2. Upload initial data to Cloudinary
3. Test admin panel login
4. Monitor logs for any errors
5. Set up uptime monitoring (optional)
